# LogisticBay Timesheets — Finding Register

> **This is the canonical registry for every `F-XX` finding ID used anywhere in
> this repository.** Going forward, an F-number is assigned here first; no
> other doc, comment, or test invents one. `STATUS.md` remains the source of
> truth for *current build state*; this file is the source of truth for *what
> each finding ID means and where it stands*.
>
> Built 2026-08-30 by reconstructing every `F-[0-9]{2}` reference from the
> working tree and the full commit history (`git log --all`, 15 commits, all
> on `main`). Two genuine ID collisions turned up during reconstruction
> (`F-03` and `F-04`, both the result of a later commit reusing an ID that was
> already closed) — both are now resolved; see **Collision history** below for
> how and why.

Status vocabulary: `OPEN` · `CLOSED` · `ACCEPTED` · `DEFERRED` · `PARTIAL` ·
`AUTHORIZED` (remediation decided and authorized by the owner, **not yet
proven**). Authorization is not closure: only executable evidence closes a
finding, and F-19's closure below records exactly what its evidence proves and
what it does not.

**`DEFERRED` requires a mandatory trigger** (owner decision, 2026-09-12;
AGENT_WORKFLOW.md §19). Every deferred entry states why it is not being fixed
now, the current risk, and the **lifecycle gate** at which it must be resolved —
`PUBLIC DEPLOYMENT GATE` · `RELEASE / APP STORE GATE` ·
`BEFORE SUBMISSION IMPLEMENTATION` · `BEFORE COMPANY ONBOARDING`. A limitation
that is intentional and will not be fixed is `ACCEPTED` with its reason, not
`DEFERRED`. Deferral without a trigger is a forgotten defect wearing a status.

**Deferred planned features are not findings.** Email verification is unbuilt
planned work governed by D24, not a defect, and deliberately has no F-number.
`F-31` remains the next free ID.

| ID | Finding | Status | Resolution / owner | Evidence |
|----|---------|--------|--------------------|----------|
| F-01 | Tenant repository boundary — reads/writes must go through `TenantContext` + `shiftRepository`, never raw Prisma in a route | CLOSED | Implemented in `8572732`; 11 Company A/B isolation tests | `DECISIONS.md:243`, `STATUS.md:108` |
| F-02 | Schema: `Shift` bound to `CompanyMembership` by one composite FK (`membershipId`+`companyId`+`userId` must agree) | CLOSED | Implemented in `4417f62` | `DEVLOG.md:113` ("Schema (F-02 closed)"), `DECISIONS.md:242` |
| F-03 | First Prisma migration / tenant-safe schema foundation (`20260830132905_init` + `invariants.sql`) | CLOSED | Implemented in `4417f62` | `DEVLOG.md:121` ("Migration (F-03 closed)") |
| F-04 | Database tenant-integrity test suite (`test:db`) brought inside the authoritative `npm run check` gate | CLOSED | `d3648cd`; re-run repeatedly during later foundation work | `DEVLOG.md:139`, `STATUS.md:103` |
| F-05 | Error envelope only masked 5xx; a handler could fabricate a "safe" 4xx (e.g. a forged `statusCode`) and have it forwarded verbatim | CLOSED | Implemented in `42d950a` — only explicitly-thrown, known-safe errors pass through unmasked now | `api/src/app.ts:54`, `api/src/errorHandling.test.ts:37,123` |
| F-06 | `JWT_SECRET` validation was length-only; a weak/repeated-character or placeholder secret could pass | CLOSED | Implemented in `9bb288f` | `api/src/lib/env.test.ts:60` |
| F-07 | Nothing forced production (or an unset `NODE_ENV`) to have working email config; could boot silently unable to send reports | CLOSED | Implemented in `4de1ed8` | `DEVLOG.md:96`, `STATUS.md:111`, `api/src/lib/env.test.ts:35` |
| F-08 | `check-rules`, the static enforcement mechanism, had no proof every rule actually fires (an unwired rule fails silently) | CLOSED | Rule engine split into `scripts/rules/engine.ts` + fixture-tested in `4de1ed8` | `DEVLOG.md:76`, `STATUS.md:110`, `api/scripts/rules/engine.test.ts:2` |
| F-09 | Outbox (`ShiftSubmitJob`) had no idempotency guarantee — no unique constraint stopped duplicate submit jobs per shift | CLOSED | `SubmitJobStatus` enum + `@@unique([shiftId])`, `4de1ed8` | `DEVLOG.md:87`, `STATUS.md:109`, `api/prisma/schema.prisma:237` |
| F-10 | Default-deny route authentication — a route must be explicitly marked `public` or it's rejected; no registration path (including a child plugin) can accidentally bypass auth | CLOSED | Implemented `55ec542` (originally mislabeled `F-04` in that commit); numbering correction `b7157b6` | `CLAUDE.md:145`, `api/src/app.ts:29`, `api/src/lib/auth.ts:7`, `api/scripts/rules/routePatterns.ts:5` |
| F-11 | Same-company driver ownership/isolation — repository methods were scoped by `companyId` alone, so a second driver in the *same* company could reach another driver's shift by ID | CLOSED | `279004f`; originally mislabeled `F-03` in that commit, relabeled `F-11` during this register's construction | `api/src/tests/db/repositoryTenantBoundary.test.ts:78,195` |
| F-12 | *reserved, unassigned* | — | — | no reference anywhere in the tree or git history |
| F-13 | Docs (README, DEVLOG, STATUS) rewritten to match observed repo state rather than aspirational state | CLOSED | `4de1ed8` | `DEVLOG.md:74,103` |
| F-14 | No production boundary converts authenticated `request.auth` into a repository `TenantContext`, and no authorization policy consumes `membershipStatus`. A future protected route could treat authentication as authorization and let an inactive membership mutate business data | CLOSED | P1.2b "Authorized Tenant Context", implemented in `2c85f4f` (RED reviewed before GREEN). `authorizeTenant(AuthContext)` in `api/src/lib/authorization.ts` is the one sanctioned production caller of `TenantContext.trust()`; it takes no client-supplied identity and performs no database read. Active → `TenantContext` preserving `companyId`, `userId` and `membershipId`; anything not exactly `"active"` → generic `403 FORBIDDEN` (D17). No generic inactive bypass exists. **No longer blocks the first protected business route** — that remains blocked by O8 alone. AUTH.md's limited inactive-member operations are still unimplemented; see STATUS.md | `api/src/lib/authorization.ts:67`; 5 tests in `api/src/lib/authorization.test.ts`; `npm run check` exit 0 at `2c85f4f` (110/110 unit, 45/45 DB, 4 migrations) |
| F-15 | Authentication runs before the rate limiter, so rate-limited traffic still performs Session and membership reads; Fastify is also created without a reviewed proxy-trust configuration for the intended Railway deployment | DEFERRED | **Trigger: PUBLIC DEPLOYMENT GATE — mandatory review before any public deployment.** Accepted temporarily; **BLOCKS public deployment.** Risk while deferred: rate-limited traffic still costs a Session and a membership read, and with no reviewed `trustProxy` every client behind the intended proxy shares one limiter bucket. See also F-28, measured at the 2026-09-12 audit and gated here. Direction (not decided): cheap pre-auth limiting, post-auth identity/route limits, reviewed proxy trust, tighter login/activation limits — exact limits and `trustProxy` value are **owner decisions** | Audit probe at `0591241`: 302 valid requests → 300 succeeded, 2 rate-limited, **all 302** caused a Session read and a membership read. Hook order: `api/src/app.ts` registers the auth `onRequest` hook before `rateLimit` |
| F-16 | One `ShiftSubmitJob` row per Shift is proven, but atomic Shift-transition-plus-enqueue, atomic worker claim, and exactly-once external email delivery are **not**. Shift transition and enqueue are currently separate repository operations | DEFERRED | **Trigger: BEFORE SUBMISSION IMPLEMENTATION — mandatory.** Defer to the submission / PDF / email boundary; **must be fixed before that feature ships.** Risk while deferred: none reachable today — no submission route exists, so nothing can enqueue. Direction (not decided): one transactional submit-and-enqueue, then safe worker claiming and external side-effect idempotency | `api/prisma/schema.prisma:303` (`@@unique([shiftId])`); F-09 proves storage uniqueness only. Supersedes the informal "submission immutability" item below |
| F-17 | Production `npm start` invokes `tsx`, which is declared under `devDependencies` — `npm ci --omit=dev` installs cleanly and then fails at runtime | DEFERRED | **Trigger: PUBLIC DEPLOYMENT GATE — mandatory.** Defer to deployment hardening; **BLOCKS public deployment.** Risk while deferred: none in development, where `tsx` is installed; a production `npm ci --omit=dev` would install cleanly and then fail at runtime. Not corrected in the 2026-09-12 closeout: the recorded direction is a build change (compile and run emitted JavaScript), which is an owner/deployment decision, not a local fix. Preferred direction: compile TypeScript and run emitted JavaScript | `api/package.json:8` (`"start": "tsx src/server.ts"`) vs `api/package.json:46` (`tsx` under `devDependencies`) |
| F-18 | `Company.joinCode` is a permanent plaintext company-wide credential: unique, but with no expiry, rotation, usage limit, hashing or guaranteed entropy | DEFERRED | **Trigger: BEFORE COMPANY ONBOARDING — mandatory.** Defer to onboarding / activation implementation; **must be fixed before that feature ships.** Risk while deferred: none reachable today — no onboarding route reads the column. Direction (not decided): high-entropy expiring invitation records storing only a digest; exact expiry and format are **owner decisions**. No onboarding route uses it today | `api/prisma/schema.prisma:61` (`joinCode String @unique`) |
| F-19 | The declared-lifetime check proves `0 < exp - iat <= 900` but does not reject a correctly signed token whose `iat` is far in the future | CLOSED | Fixed in Registration Increment 1, the increment whose token minting was the stated trigger. **`iat` must not exceed server-now by more than 60 seconds** — a clock-skew allowance between this product's own minter and verifier, nothing more. Enforced in `hasValidTiming` (`api/src/lib/auth.ts`), which BOTH pipelines call, so the tenant and identity kinds cannot drift apart on timing. A JWT rule only: unrelated to D20, under which a driver's *declared* Start/Finish time may be any instant, past or future. **Closes exactly this bound and nothing wider** — the deferred negative matrix (wrong-*value* `iss`/`aud`, `alg:none`, a missing membership row) is unaffected, and this is not a claim that token security is complete | Written RED first: `api/src/lib/auth.test.ts` case 19 failed `200 !== 401` against the unfixed verifier. GREEN asserts all four points — present-dated token 200, `iat = now + 30s` **accepted** (skew is not an attack), `iat = now + 24h` with `exp = iat + 900` **401** with the canonical `{"error":"Not authenticated","code":"UNAUTHENTICATED"}` and no temporal disclosure. The same four hold for the identity kind (`api/src/routes/auth.test.ts` R19). Proven load-bearing by ephemeral mutation: deleting the bound failed case 19 and R19 and nothing else (37 pass / 2 fail), and `api/src/lib/auth.ts` was restored byte-identical (SHA-256 `8bcfd616…52c1a`). Tenant auth regression green throughout — cases 1–18 unchanged |
| F-20 | `Shift.notes` and `ShiftSegment.notes` have no defined source or privacy audience. Private driver context could later become employer-visible through the PDF or a company screen, crossing the CLAUDE.md privacy boundary | OPEN | **Owner decision required before any exposure.** Removal remains a valid option. No route, form or PDF exposes them today. Graduates the previously informal item below to a canonical ID | `api/prisma/schema.prisma:213` (`Shift.notes`), `api/prisma/schema.prisma:266` (`ShiftSegment.notes`); nothing reads either |
| F-21 | Database uniqueness protects `refreshTokenHash` and `previousRefreshTokenHash` separately, but nothing prevents one Session's current hash equalling another Session's previous hash — future refresh lookup semantics could be ambiguous | CLOSED | Resolved in the complete-authentication phase (2026-09-11) by the owner-approved **lookup-discipline** direction, with **no schema change** — still 8 migrations, `Session` untouched. The ambiguous query is now **structurally unrepresentable**: `RefreshDatabase` in `api/src/repositories/refreshRepository.ts` declares `session.findUnique` and `session.updateMany` and NOTHING else — no `findFirst`, no `findMany`, no `$queryRaw` — and `findUnique`'s `where` is a union of the two single-digest keys, each backed by its own unique index. An `OR` across both columns does not typecheck. `resolve()` owns the PRECEDENCE (current first, previous only if current missed), so no caller receives a "search both" primitive to misuse. **Closes exactly the lookup ambiguity, and nothing wider**: the cross-column state is still *permitted* by the database, and this finding's closure is a claim about resolution being deterministic, not about the state being impossible. Two related properties were implemented alongside and are NOT part of this ID: atomic conditional rotation, and the one-generation limit of reuse detection (the schema holds a single previous digest, so a credential two or more rotations old is answered as unknown rather than as reuse — documented in `api/src/services/refresh.ts` and in AUTH.md) | **Crafted-collision tests** in `api/src/tests/db/refresh.test.ts`: F1 builds the exact state (one digest matching two Session rows, confirmed by an `OR` query written in the TEST — the query production cannot express) and asserts `resolve()` returns the CURRENT holder with `matched === "current"`; F2 repeats it 8 times for stability against planner choice; F3 proves the two possible precedences give OPPOSITE observable outcomes end to end and that ours is the one that occurs. **Proven load-bearing by reverted mutation**: reversing the precedence in `resolve()` failed F1, F2 and F3 and nothing else (39 pass / 3 fail); `refreshRepository.ts` restored byte-identical (SHA-256 `d13f71a6…8450`). The adversarial test also found and fixed a real defect before GREEN: rotation writes the presented digest into the unique `previousRefreshTokenHash` column, so a crafted collision raised P2002 and escaped as a **500** — a different answer from every other refresh failure, and therefore an oracle. It now fails closed to the canonical 401 (`appliedOrNot`). Root `npm run check` exit 0 (177 api, 100 mobile, 146 DB, 8 migrations)

| F-22 | `TenantContext`'s nominal brand is compile-time only. TWO defects under one ID: (a) the instance was not frozen, so a holder could repoint `companyId` after construction; (b) `as unknown as TenantContext` forges one without calling `trust()` | PARTIAL | **(a) FIXED in `c742b03`** — `Object.freeze(this)` as the last statement of the constructor; an authority field can no longer be reassigned, redefined, extended or deleted, and a write throws rather than silently no-op'ing (ESM is strict mode). Permanently tested. **(b) OUTSTANDING, and not fixable in TypeScript** — no construct prevents a double cast. It is no longer *contradicted* by the source: `tenantContext.ts`'s class comment now says SANCTIONED rather than only, states the cast plainly, names the one production call site, and re-anchors the guarantee to D16 (repository boundary + database composite keys). **Owner disposition still required on (b):** whether it stays outstanding work, or is recorded as `ACCEPTED` under D16 as a limit of type-level guardrails rather than a defect. This audit and this fix deliberately did not decide that. Does NOT invalidate P1.2b — `authorizeTenant` remains the only sanctioned production caller of `trust()`. Blocks nothing | (a) RED before GREEN: 3 of 4 tests in `api/src/lib/tenantContext.test.ts` failed before the fix (`Reflect.set` returned `true` and the company changed); 4/4 after. The exact audit probe that found F-22 was re-run against the fixed module and passes. `npm run check` exit 0 at `c742b03` (114/114 unit, 45/45 DB, 4 migrations); CI `completed/success` for that SHA. (b) `tsc --noEmit --strict` exit 0 on a probe containing `{…} as unknown as TenantContext`, while the adjacent `@ts-expect-error` on a plain object literal WAS satisfied — the brand stops accidents, not intent; `runRules()` reported zero violations for the forgery |
| F-23 | The `tenant-context-trust-sites` exemption `lib[/\\]auth` is an unanchored path substring, so any `lib` directory whose file begins `auth` is exempt — including `src/services/lib/authX.ts` and `src/routes/lib/authX.ts` | OPEN | **Owner decision required.** Found by the independent P1.2b audit of `60effc9` (2026-08-31). A guardrail gap, not a runtime gap (D16) — but AUTH.md and `STATUS.md` describe a ban the rule does not deliver. Direction (not decided): anchor the exemption to `^src[/\\]lib[/\\]auth` and add a bad fixture at `src/services/lib/authX.ts`. Do not widen F-14 for it. Blocks nothing | `runRules()` against a scratch fixture tree: a service at `src/services/lib/authSneak.ts` calling `TenantContext.trust({ companyId: companyIdFromClient, … })` produced **zero** violations, while the control `TenantContext.trust` in `src/routes/leak.ts` was correctly flagged. Same run: bracket access, a method alias and a destructure of `trust` all evaded the rule in a plain route file. Rule at `api/scripts/rules/engine.ts:278` |
| F-24 | The two checks binding an authenticated identity to its user — `session.userId === sub` and `membership.userId === sub` — have no executable proof anywhere in the suite | CLOSED | Regression evidence added in `597bd1110db4675602711da7840890c83d92968c` as cases 17 and 18 of `api/src/lib/auth.test.ts` — the only file that commit touched. Found by the independent P1.2b audit of `60effc9` (2026-08-31). Behaviour was CORRECT throughout and **production auth code is unchanged**: this closed a coverage gap, not a defect. Each case leaves every *other* check satisfied, so the rejection is attributable to the binding under test, and both assert the frozen generic failure — `401 { "error": "Not authenticated", "code": "UNAUTHENTICATED" }`, no AuthContext, no tenant authority — so the boundary remains a non-oracle for which check failed. The escape this now guards is unchanged: without `auth.ts:159` any holder of a valid session could present any `membershipId` and receive a full `TenantContext` for that company. **Closes exactly these two bindings and nothing wider.** Still uncovered from the same audit batch, and NOT part of this closure: wrong-*value* `iss`/`aud`, `alg:none`, and a missing membership row — those remain with the deferred P1.2a negative matrix | Both cases proven independently load-bearing by ephemeral mutation RED in the working tree: deleting `api/src/lib/auth.ts:155` (`session.userId !== claims.sub`) failed case 17 alone (`200 !== 401`) while case 18 stayed green; deleting `:159` (`membership.userId !== claims.sub`) failed case 18 alone (`200 !== 401`) while case 17 stayed green. Both mutations were reverted and never committed — `auth.ts` restored byte-identical (SHA-256 `25b91f68…61eba`), and the commit's file list contains no production source. Targeted GREEN 2/2; `api/src/lib/auth.test.ts` 18/18; the authoritative gate ran exit 0 on the tree committed as `597bd111` (127/127 unit, 50/50 DB, 5 migrations); GitHub Actions run `34368029271` `completed/success` for that exact SHA || F-25 | The identity pipeline's session-ownership binding (`session.userId === claims.sub` in `requireSession`) had **no effective regression coverage**. `R16` in `api/src/routes/auth.test.ts` claimed to prove it and did not: its test double returned no row for the foreign user, so with the production check deleted the request fell through to `accountView`'s own account-not-found path, which throws a **byte-identical** `401`. The assertion was satisfied either way | CLOSED | Found by the independent authentication audit of `049cc37` (2026-09-12); corrected in this closeout under AGENT_WORKFLOW §3.1. **Production authentication code is unchanged and was correct throughout** — this closed a coverage gap, not a defect. The correction is test-only: a second genuinely existing account (`otherUserRow()`) is now returned by the double, so deleting the binding makes the request **succeed** and disclose that account instead of failing for an unrelated reason. Same class as F-24, which closed the equivalent gap for `requireAuth`'s two bindings but did not cover the identity pipeline. Closes exactly this binding and nothing wider | **Load-bearing by ephemeral mutation:** corrected `R16` GREEN (20/20); deleting only `api/src/lib/auth.ts`'s `requireSession` binding failed **R16 alone** with `expected 401, actual 200` — the request returned the foreign account — while the other 19 cases stayed green; `auth.ts` restored byte-identical (SHA-256 `8bcfd616…52c1a`) and re-run 20/20. Against a real database the same mutation made `GET /auth/me` return the victim's name and email and `POST /auth/logout` revoke the victim's session. Exploiting it requires minting a token, i.e. `JWT_SECRET`, so the risk was silent regression with CI green, not a live hole |
| F-26 | Two conditional `where` predicates in `api/src/repositories/refreshRepository.ts` had **no regression coverage**, because the service's own JavaScript checks answer first and shield them from the HTTP surface: `rotateFromGrace`'s `previousRefreshTokenGraceUntil: { gt: now }` (the TOCTOU guard) and `rotateCurrent`'s `refreshTokenHash: presentedDigest` (the atomicity guard). Deleting either left the entire suite green | CLOSED | Found by the independent authentication audit of `049cc37` (2026-09-12); corrected in this closeout under AGENT_WORKFLOW §3.1. **Production refresh code is unchanged and was correct throughout.** Both are now proven at the repository level rather than through a race: a race cannot force the interleaving that would expose a missing predicate, so `now` and the presented digest are varied directly instead. `R15` proves the grace deadline is enforced by the database; `C4` proves `rotateCurrent` applies only to the digest the caller presented. Closes exactly these two predicates and nothing wider | **Load-bearing by ephemeral mutation, one predicate at a time.** `R15`: baseline 147/147 GREEN; deleting only the grace predicate failed **R15 alone** with `expected false, actual true` (the expired rotation applied). `C4`: baseline 148/148 GREEN; deleting only the presented-digest condition failed **C4 alone** with `expected false, actual true` (a caller acting on a superseded digest rotated the row). `refreshRepository.ts` restored byte-identical after each (SHA-256 `d13f71a6…8450`) |
| F-27 | AUTH.md over-specified refresh concurrency — *"Two simultaneous refreshes of one credential cannot both rotate: exactly one matches, and the loser is refused"* — and `C1`/`C2` asserted that literal outcome (`[200, 401]`). Measurement shows two concurrent refreshes **may both return 200**: when the first commits before the second resolves, the second finds the presented digest in the PREVIOUS column and legitimately succeeds through grace recovery, which is the mechanism AUTH.md itself provides. The tests were asserting a timing accident, and `C1` was therefore capable of failing spuriously | CLOSED | Found by the independent authentication audit of `049cc37` (2026-09-12). **AUTH.md clarification explicitly authorized by the owner**; it states the invariant the design always had and changed no behaviour. The frozen contract now says: only one request may rotate a credential as CURRENT; a concurrent request may legitimately succeed through grace recovery; concurrency must never produce two usable lineages, a forked Session, a moved `expiresAt` or an incoherent row. `C1` and `C2` were rewritten to assert that property instead of the status pair, and `C3` already did. `STATUS.md`'s refresh row reconciled in the same commit. **Production refresh code is unchanged** | **Measured, not argued:** `[200, 200]` observed 1/30 at n=2 on a warm shared app and 4/40 across n=2…8; routine at n≥3 (3-of-5 and 5-of-8 seen). The security invariant held in **40/40** concurrent rounds — never more than one usable credential, no session fork, no revocation, no crossed `sub`/`sessionId`. Correcting `C1` removed the incidental protection its status assertion gave to `rotateCurrent`'s atomicity, which is why that predicate is now covered deterministically by `C4` under F-26 — the invariant is proven at least as strongly as before, and no longer by timing luck (AGENT_WORKFLOW §10) |
| F-28 | `bcryptjs` is a pure-JavaScript bcrypt, so it runs **on** the single Node event loop rather than on libuv's threadpool. Every anonymous `POST /auth/login` and `POST /auth/register` therefore buys ~one cost-12 hash of the server's only thread, and the configured rate limit permits more of that work than a minute contains | DEFERRED | **Trigger: PUBLIC DEPLOYMENT GATE — mandatory review before any public deployment** (AGENT_WORKFLOW §19). Belongs with F-15, whose rate-limit values and `trustProxy` configuration are owner decisions; without `trustProxy` every client behind the intended Railway proxy also shares one limiter bucket, which compounds it. Deliberately NOT piecemeal-fixed here: changing the hashing library, the cost factor or the limiter design in a test-and-documentation closeout would be exactly the speculative architecture AGENT_WORKFLOW §25 forbids. Direction (not decided): limits chosen against the CPU budget · a native or worker-thread bcrypt · re-measure cost 12 on the deployment target, which `api/src/lib/password.ts` already asks for. **Availability only** — no confidentiality or integrity impact, and nothing about it is reachable before deployment | **Measured during the audit of `049cc37`:** one cost-12 hash took 238 ms, during which a 5 ms interval timer fired **3×** instead of ~47×. A 25-request anonymous login burst degraded `GET /health` from a 1.3 ms to a 121.5 ms median — **94×**. The limiter's 300/min × 238 ms demands **~71 s of main-thread CPU per 60 s of wall clock** from a single bucket. Both endpoints are public: login spends a full verification even on an unknown email (the dummy hash that closes the timing oracle, correct in itself), and registration performs a full hash |
| F-29 | A JWT whose `aud` claim is an **array** containing both audiences satisfies both verifiers, so one token is accepted at tenant routes, identity routes and `/auth/switch-company` simultaneously. This qualifies AUTH.md's *"one token never silently acquires a second meaning"* | ACCEPTED | **Accepted 2026-09-12 by owner decision, with reason recorded.** Producing such a token requires `JWT_SECRET`; an attacker holding the signing secret can already mint any token of either kind, so the separation defends nothing additional against them. The product's own minters emit a single string `aud` at both sites (`api/src/lib/tokens.ts`), and no client-supplied input reaches the claim — every DTO is `.strict()`. Tightening it would mean re-validating `aud` in the Zod claims schemas, duplicating a decision the verifier already owns and breaking CLAUDE.md's "one decision, one place". **No production change.** Revisit only if a future design gives a party the secret without full authority | Audit of `049cc37`: a forged `aud: ["timesheets-api", "timesheets-identity"]` token was accepted by `GET /shifts/current`, `GET /auth/me` and `POST /auth/switch-company`; the same token signed with any other secret was refused `401`; both shipped minters verified to emit a single string audience; `POST /auth/login` refuses an `aud` field in the body with `400` |
| F-30 | Mobile logout clears the local refresh credential even when the server revocation cannot be reached, and there is **no retry path anywhere in the app**. The server Session therefore stays unrevoked, with its refresh credential valid until the 90-day absolute expiry | ACCEPTED | **Accepted 2026-09-12 by owner decision as intentional local-first V1 behaviour, with reason recorded.** The local clear is correct and must not become conditional: a driver who taps sign out in a yard with no signal has to be signed out of the device immediately, and the client never claims the server revocation succeeded. Exploiting the residual requires an attacker to **already possess** the refresh credential — extracted from SecureStore via a jailbroken/rooted device or a malicious backup — so it is not reachable from the logout itself. A retry design is genuinely non-trivial rather than merely unwritten: revoking later requires retaining revocation authority on a device whose user has just asked to be logged out, which reintroduces the exposure it would close. Not improvised here (AGENT_WORKFLOW §25). Owner may later choose a revocation design; until then this is the accepted limitation | `mobile/src/auth/AuthContext.tsx:259` awaits `logoutSession` and discards its result; a repository-wide search for a retry/queue/outbox mechanism in `mobile/app` and `mobile/src` returns nothing. The existing test *"logout with NO NETWORK still signs the driver out locally, immediately"* asserts the local clear and correctly does **not** claim server revocation. Audit of `049cc37` confirmed the device is fully logged out locally in every case |

## Known sub-issues tracked without their own ID

- **A crafted F-21 cross-column collision denies the rightful credential holder** (independent authentication audit of `049cc37`, 2026-09-12; audit severity Informational). When one Session's current digest equals another's previous digest, `rotateCurrent`'s write into the unique `previousRefreshTokenHash` column raises `P2002`, which `appliedOrNot` collapses to the canonical `401` — so the legitimate holder is refused rotation rather than the wrong Session being rotated. **Fail-closed, and neither Session is mutated**, which the audit confirmed directly. Reaching the state requires a 256-bit CSPRNG collision, so it is not a production path. **Accepted as informational; no ID and no architecture to build for it** — F-21's closure already records that the cross-column state remains permitted by the schema and that the claim is about deterministic resolution, not impossibility.

- **`routePatterns.ts` paren-depth matcher blind spot** (surfaced during F-10's adversarial review): the static `route-declares-auth` check can be defeated by an unbalanced `(` inside a string/template literal in a route handler, causing a false negative. Does **not** affect the actual runtime boundary (the `onRequest` hook in `app.ts`, which has no such gap). Recorded as a cleanup item in `STATUS.md`'s "Known limitations / cleanup backlog" section, not as a numbered finding.

- **`db-check` can force-drop a fixed database name on whatever server `DATABASE_URL` points at** (backend foundation audit, 2026-08-31; audit severity Low). `api/scripts/db-check.ts:56` issues `DROP DATABASE IF EXISTS "lb_timesheet_check" WITH (FORCE)`; the naming convention is not a structural safety barrier the way `db-smoke`'s explicit `NODE_ENV` + database-name guard is. **Non-blocking backlog** — recorded in `STATUS.md`'s cleanup backlog, not given an ID. Direction: mirror the stronger guards already used elsewhere, or require an explicit destructive-test sentinel.

- **`AuthStore` over-fetches the Session row** (backend foundation audit, 2026-08-31; audit severity Low). `api/src/lib/authStore.ts:62` selects the whole row — refresh-token hashes included — and narrows it in memory. Defence in depth, not a demonstrated leak: the adapter rebuilds a four-field record and nothing above it sees Prisma. **Non-blocking backlog** — recorded in `STATUS.md`'s cleanup backlog. Direction: an explicit Prisma `select` at the query boundary, when that boundary is next touched under owner authorization.

- **Static rules remain guardrails, by design** (backend foundation audit, 2026-08-31; audit severity Observation). The audit confirmed the 17 checks are pattern/text based and bypassable in some syntax classes, with runtime auth, `TenantContext` typing, repository boundaries, database constraints and integration tests as the compensating controls. **Accepted as-is** — this is D16 working as intended, not a defect, and no finding ID was created for it.

## Gaps in numbering

- **F-12 is reserved and unused.** No occurrence anywhere in the current working tree or in any commit across the full `git log --all` history.
- **Informal items awaiting a formal ID** — updated 2026-08-31, after the STATUS.md accuracy pass the original note was waiting on. Of the four: the `Shift.notes` / `ShiftSegment.notes` privacy question is now **F-20**; submission immutability is now **F-16**; tooling-count drift was reconciled in `0591241`. **`/health` readiness semantics remains informal** — no ID, no repository evidence assigning one.
- The audit source documents (`audits/phase-0-audit.md`, `audits/phase-0-fixes-review.md`) use their own independent numbering (`1.1`–`8.10`) and contain **zero** `F-XX` references. The `F-` numbering is an independently-assigned implementation-ticket scheme, not a restatement of the audit's own section numbers.
- The same separation applies to the **backend foundation audit of `0591241` (2026-08-31)**, which produced F-14…F-21. That audit's report-local identifiers (`AUDIT-2026-*`) are **not** canonical IDs and are not used anywhere in this repository. Its full report is not held under `audits/` — only the owner-accepted dispositions were supplied to the repository, and those are recorded in the table above rather than reconstructed as an audit artifact. If the report itself is added later it belongs in `audits/`, under that directory's existing naming.

## Collision history (resolved)

### `F-03` — first migration vs. same-company isolation

`DEVLOG.md:121` closed `F-03` as the first Prisma migration (commit `4417f62`) — this is the **older, historical assignment and stays canonical.** A later commit, `279004f` ("fix: scope driver-facing repository access by company AND owning membership"), reused `F-03` in `api/src/tests/db/repositoryTenantBoundary.test.ts` for an unrelated finding: repository methods scoped by `companyId` alone allowed one driver to reach another driver's data *within the same company*. That was a numbering mistake, not two names for the same finding — a migration-generation task and a tenant-isolation security bug have nothing in common beyond sharing a label.

**Resolution applied in this pass:** `DEVLOG.md:121` was left untouched (historical record, and it was never wrong — it always meant the migration). The two live references in `api/src/tests/db/repositoryTenantBoundary.test.ts` (lines 78 and 195) that incorrectly reused `F-03` for the same-company-isolation finding were changed to `F-11`, a confirmed-unused ID. `F-11`'s finding is `CLOSED` — the fix already shipped in `279004f`; only the label was wrong. Verified after the rename: `tsc --noEmit` clean, `eslint .` clean, `git diff --check` clean (comment/test-description-only diff, no logic touched).

### `F-04` — test:db-in-check vs. default-deny routing

`STATUS.md:103` / `DEVLOG.md:139` close `F-04` as bringing the `test:db` suite inside `npm run check` (commit `d3648cd`) — this is the **canonical, correct meaning and stays `CLOSED`.** The phrase "F-04 closed pending a green run" was provisional wording at the time of that commit; the authoritative gate has since run green with `test:db` incorporated, and the DB/integrity suites have been re-run repeatedly during later foundation work, so this finding is not technically partial. The stale "not yet re-verified green" CI wording elsewhere in `STATUS.md` is a documentation-accuracy issue to be corrected in the upcoming STATUS.md accuracy pass — it is not evidence against `F-04`'s closure.

Separately, commit `55ec542` (this session) originally mislabeled the *default-deny routing* feature as `F-04`, colliding with the above. That was caught and corrected within the same session: commit `b7157b6` relabeled all 8 of those references to `F-10`. No working-tree file currently uses `F-04` for anything but the `test:db` finding.

### Why this matters going forward

Both collisions above share the same root cause: a later commit assigned "the next number" without checking this register (which didn't exist yet) for what was already taken. Now that `FINDINGS.md` is canonical, any new finding gets an ID from here first. **F-14…F-21 were assigned here on 2026-08-31** from the backend foundation audit, in one pass, after re-verifying every `F-XX` in the working tree and in `git log --all`. **F-12 was NOT used**: the table above still marks it reserved, and reserved is not the same as next-available. **F-22…F-24 were assigned here on 2026-08-31** from the independent P1.2b audit of `60effc9`, in one pass, after re-verifying every `F-XX` in the working tree. **F-25…F-30 were assigned here on 2026-09-12** from the independent authentication audit of `049cc37`, in one pass. That audit used report-local identifiers (`AUTH-AUDIT-01`…`AUTH-AUDIT-08`) which are **not** canonical and appear nowhere in this repository; the mapping is F-25 ← AUTH-AUDIT-01, F-26 ← AUTH-AUDIT-04 plus the atomicity half of AUTH-AUDIT-03a, F-27 ← AUTH-AUDIT-03a/03b, F-28 ← AUTH-AUDIT-02, F-29 ← AUTH-AUDIT-05, F-30 ← AUTH-AUDIT-06. Deliberately **not** given IDs: AUTH-AUDIT-07 (a stale Prisma comment, corrected in the same session under AGENT_WORKFLOW §3.1 and recorded in DEVLOG) and AUTH-AUDIT-08 (informational, recorded below without a number) — findings are not created mechanically. **F-31 is the next free ID.**
