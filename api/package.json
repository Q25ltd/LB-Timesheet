{
  "name": "lb-timesheet-api",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "postinstall": "prisma generate",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "prisma studio",
    "seed": "tsx scripts/seed.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx/esm --test --test-timeout=30000 $(find src scripts -name \"*.test.ts\" -not -path \"*/tests/db/*\" -not -path \"*__fixtures__*\")",
    "check": "npm run typecheck && npm run check:rules && npm run validate && npm test",
    "check:rules": "tsx scripts/check-rules.ts",
    "validate": "prisma validate",
    "lint": "eslint .",
    "test:db": "tsx scripts/db-check.ts",
    "migrate:baseline": "prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script",
    "test:db:run": "node --import tsx/esm --test --test-timeout=60000 $(find src/tests/db -name \"*.test.ts\")",
    "db:smoke": "tsx scripts/db-smoke.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/jwt": "^10.0.0",
    "@fastify/rate-limit": "^10.3.0",
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.5.0",
    "@sendgrid/mail": "^8.1.6",
    "@types/pg": "^8.23.1",
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.2.3",
    "fastify": "^5.8.2",
    "pdfkit": "^0.18.0",
    "pg": "^8.23.0",
    "prisma": "^7.5.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^25.6.0",
    "@types/pdfkit": "^0.17.5",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
