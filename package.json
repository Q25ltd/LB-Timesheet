{
  "name": "lb-timesheet",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix api",
    "typecheck": "npm run typecheck --prefix api",
    "test": "npm test --prefix api",
    "check:rules": "npm run check:rules --prefix api",
    "knip": "knip",
    "check": "npm run typecheck && npm run lint && npm run check:rules && npm run validate:schema && npm run knip && npm test",
    "lint": "eslint .",
    "validate:schema": "npm run validate --prefix api"
  },
  "devDependencies": {
    "knip": "^6.14.2",
    "@eslint/js": "^9.39.0",
    "eslint": "^9.39.0",
    "typescript-eslint": "^8.46.0"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "type": "module"
}
