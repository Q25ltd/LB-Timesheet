/** The My Records tab. Wiring only — the screen owns the rendering. */
import { RecordsScreen } from "../../../src/screens/RecordsScreen";

export default function RecordsRoute() {
  return <RecordsScreen />;
}
